spring:
  profiles:
    active: local
    group:
      local: local, common, db
      blue: blue, common, db
      green: green, common, db

server:
  env: blue

---

spring:
  config:
    activate:
      on-profile: local

server:
  port: 8080
  serverAddress: localhost

serverName: local_server

---

spring:
  config:
    activate:
      on-profile: blue

server:
  port: 8080
  serverAddress: ${Elastic_URL}

serverName: blue_server

---
spring:
  config:
    activate:
      on-profile: green

server:
  port: 8081
  serverAddress: ${Elastic_URL}

serverName: green_server

---



